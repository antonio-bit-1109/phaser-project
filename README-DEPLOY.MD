dopo una modifica al codice, per rideployare il progetto seguire i seguenti passi:

1- npm run build
2- npm run deploy (questo comando fa il push sul branch gh-pages) dalla pagina di git pages è selezionato quel branch
come source